CREATE TABLE `employee` (
	`id` integer PRIMARY KEY NOT NULL,
	`name` text NOT NULL,
	`joinedAt` integer NOT NULL
);
--> statement-breakpoint
CREATE TABLE `user` (
	`id` integer PRIMARY KEY NOT NULL,
	`name` text NOT NULL
);
